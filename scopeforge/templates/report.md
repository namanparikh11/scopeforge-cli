# ScopeForge Security Scan Report

**Project:** {{ scan.project }}  
**Scan Name:** {{ scan.scan_name or "Auto timestamped scan" }}  
**Nmap Version:** {{ scan.nmap_version or "Unknown" }}  
**Scan Folder:** `{{ scan.scan_dir }}`

## Executive Summary

This report identified **{{ stats.live_hosts }}** live host(s) and **{{ stats.open_ports }}** open service(s). **{{ stats.high_risk_services }}** service(s) were flagged as high-priority review items based on transparent exposure rules.

No exploitation is performed by ScopeForge. Findings are based on Nmap discovery/service output and should be treated as review priorities unless further validation confirms a vulnerability.

## Scan Proof & Metadata

**Command executed:**

```bash
{{ scan.command or "Command unavailable in XML metadata" }}
```

- **Start:** {{ scan.start_time or "Unknown" }}
- **End:** {{ scan.end_time or "Unknown" }}
- **Elapsed:** {{ scan.elapsed or "Unknown" }}

Generated evidence files:

- `raw/scan.nmap`
- `raw/scan.xml`
- `raw/scan.gnmap`
- `exports/report.html`
- `exports/report.md`
- `exports/services.csv`
- `exports/parsed-results.json`

## Dashboard Summary

| Metric | Value |
|---|---:|
| Hosts scanned | {{ stats.hosts_scanned }} |
| Live hosts | {{ stats.live_hosts }} |
| Open ports | {{ stats.open_ports }} |
| High-risk services | {{ stats.high_risk_services }} |

## Risk Distribution

| Risk | Count |
|---|---:|
{% for risk, count in stats.risk_counts.items() -%}
| {{ risk }} | {{ count }} |
{% endfor %}

## Host Summary

| Host | Hostnames | Status | Open Ports | OS Guesses |
|---|---|---|---:|---|
{% for host in scan.hosts -%}
| `{{ host.address }}` | {{ host.hostnames|join(', ') or '—' }} | {{ host.status }} | {{ host.ports|selectattr('state','equalto','open')|list|length }} | {{ host.os_matches|join('; ') or '—' }} |
{% endfor %}

## Services Inventory

| Host | Port | Service | Product / Version | Risk | Recommendation |
|---|---:|---|---|---|---|
{% for port in scan.all_ports -%}
| `{{ port.host }}` | {{ port.port }}/{{ port.protocol }} | {{ port.service }} | {{ [port.product, port.version, port.extrainfo]|select|join(' ') or '—' }} | {{ port.risk }} | {{ port.recommendation }} |
{% endfor %}

## Priority Findings

{% set findings = scan.all_ports|selectattr('risk','in',['Critical','High','Medium'])|list %}
{% if findings %}
{% for port in findings %}
### {{ port.service|upper }} exposure on {{ port.host }}:{{ port.port }}

- **Severity:** {{ port.risk }}
- **Evidence:** `{{ port.port }}/{{ port.protocol }} {{ port.state }} {{ port.service }} {{ port.product }} {{ port.version }}`
- **Impact:** {{ port.reason }}
- **Recommendation:** {{ port.recommendation }}

{% endfor %}
{% else %}
No high or medium review items were generated by the current risk rules.
{% endif %}

## Limitations & Disclaimer

This report is based on Nmap discovery/service output and ScopeForge risk rules. It does not confirm vulnerabilities unless they are manually validated. No exploitation, brute force, malware, stealth scanning, or intrusive testing is performed by ScopeForge itself.

Use only on systems you own or have explicit written permission to assess. The user is responsible for lawful and authorized use.
